<!DOCTYPE html>
    <html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Home</title>
    </head>
    <body>
        <div class="container">
            <div class="navbar">
                <%- include('navbar', {links: links}) %>
            </div>

            <div class="header">
                <%- include('header') %>
            </div>
            

            <main class="dashboard">
                <div class="card">
                    <h2>Total Muscles</h2>
                    <p><%= musclesCount %></p>
                </div>

                <div class="card">
                    <h2>Total Exercises</h2>
                    <p><%= exercisesCount %></p>
                </div>

                <div class="card">
                    <h2>Total Relations</h2>
                    <p><%= pairsCount %></p>
                </div>
            </main>
        </div>

    </body>
</html>